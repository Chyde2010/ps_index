name: Monthly Ps Index Signal

on:
  schedule:
    # Runs at 09:00 UTC on the 1st of every month
    - cron: '0 9 1 * *'
  workflow_dispatch:
    # Also allows manual triggering from GitHub UI
    # useful for testing and for missed months

jobs:
  compute-signals:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PS_INDEX_PAT }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install requests pandas numpy scipy

      - name: Compute monthly Ps signals
        env:
          PS_INDEX_PAT: ${{ secrets.PS_INDEX_PAT }}
        run: |
          python live_track_record/scripts/monthly_signal.py

      - name: Commit and push signal readings
        run: |
          git config user.email "github-actions@github.com"
          git config user.name "Ps Index Bot"
          git add live_track_record/signals/
          git diff --staged --quiet || git commit -m             "Auto: Monthly Ps signal reading $(date +'%Y-%m-%d')"
          git push
